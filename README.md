# mac_scripts
Collection of macOS scripts. 

- MacGrab.sh is written in Bash, so no third-party dependences. It automagically creates a snapshot, mounts and then copy's all data into a SPARSE container at your chosen target location (this is easily changeable). More options are in the works.
